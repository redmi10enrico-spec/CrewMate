# Fase 0 — Setup & fondamenta — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trasformare lo scheletro statico CrewMate Network in un monorepo pnpm/Turborepo con Next.js 15 (web + admin), un design system condiviso (`packages/ui`) portato dai token CSS esistenti, e un client Supabase tipizzato (`packages/db`) — senza ancora collegare progetti Supabase/Vercel reali.

**Architecture:** Monorepo pnpm workspaces orchestrato da Turborepo. `packages/ui` e `packages/db` sono librerie TypeScript sorgente (nessuna build separata, consumate via `transpilePackages` in Next.js). `apps/web` e `apps/admin` sono due app Next.js 15 (App Router) indipendenti che importano `packages/ui` per lo stile/i componenti e `packages/db` per il client Supabase.

**Tech Stack:** pnpm workspaces, Turborepo 2, Next.js 15 (App Router, React 19), TypeScript 5 strict, Tailwind CSS 3, Vitest + Testing Library, ESLint 9 (flat config), @supabase/supabase-js.

**Spec:** `docs/superpowers/specs/2026-08-17-fase0-setup-design.md`

## Global Constraints

- Le pagine HTML statiche esistenti in root (`index.html`, `shop.html`, `forum.html`, `candidature.html`, `assets/`) NON vengono toccate in questo piano.
- Nessuna credenziale Supabase/Vercel reale in questa fase: tutto ciò che serve un progetto reale va in `.env.example` (mai `.env`) o in `docs/SETUP.md` come istruzione manuale.
- TypeScript strict ovunque (eredita da `tsconfig.base.json`).
- Package manager: pnpm (già installato, v11.0.8). Node già installato, v24.14.1.
- Ogni package/app ha i propri script `lint`, `test` (dove applicabile), `typecheck`, orchestrati da Turborepo dalla root.

---

### Task 1: Scaffolding root del monorepo

**Files:**
- Create: `pnpm-workspace.yaml`
- Create: `package.json`
- Create: `turbo.json`
- Create: `tsconfig.base.json`
- Create: `.gitignore`

**Interfaces:**
- Consumes: niente (primo task).
- Produces: workspace pnpm funzionante (`pnpm-workspace.yaml` con pattern `apps/*`, `packages/*`); `tsconfig.base.json` che ogni package/app successivo estende con `"extends": "../../tsconfig.base.json"`; script root `build`/`dev`/`lint`/`test`/`typecheck` che girano `turbo run <task>`.

- [ ] **Step 1: Crea `pnpm-workspace.yaml`**

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

- [ ] **Step 2: Crea `package.json` root**

```json
{
  "name": "crewmate-network",
  "private": true,
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 3: Crea `turbo.json`**

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
```

- [ ] **Step 4: Crea `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "declaration": false,
    "jsx": "react-jsx"
  }
}
```

- [ ] **Step 5: Crea `.gitignore`**

```
node_modules/
.next/
.turbo/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
```

- [ ] **Step 6: Verifica che il workspace si installi correttamente**

Run: `pnpm install`
Expected: completa senza errori (nessun package oltre alla root ancora, ma valida `pnpm-workspace.yaml` e crea `pnpm-lock.yaml`).

- [ ] **Step 7: Commit**

```bash
git add pnpm-workspace.yaml package.json turbo.json tsconfig.base.json .gitignore pnpm-lock.yaml
git commit -m "chore: scaffolding root del monorepo pnpm/Turborepo"
```

---

### Task 2: `packages/ui` — setup, design tokens e Tailwind preset

**Files:**
- Create: `packages/ui/package.json`
- Create: `packages/ui/tsconfig.json`
- Create: `packages/ui/eslint.config.mjs`
- Create: `packages/ui/vitest.config.ts`
- Create: `packages/ui/vitest.setup.ts`
- Create: `packages/ui/src/theme/tokens.css`
- Create: `packages/ui/src/tailwind-preset.ts`
- Test: `packages/ui/src/tailwind-preset.test.ts`
- Create: `packages/ui/src/index.ts`

**Interfaces:**
- Consumes: `tsconfig.base.json` (Task 1).
- Produces: `tailwindPreset` (named + default export da `packages/ui/src/tailwind-preset.ts`, re-esportato da `packages/ui/src/index.ts`) — usato da `apps/web/tailwind.config.ts` e `apps/admin/tailwind.config.ts` (Task 5, 6). File CSS `packages/ui/src/theme/tokens.css` — importato via path relativo da `apps/web/app/globals.css` e `apps/admin/app/globals.css` (Task 5, 6).

- [ ] **Step 1: Crea `packages/ui/package.json`**

```json
{
  "name": "@crewmate/ui",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.0.2",
    "eslint": "^9.17.0",
    "jsdom": "^25.0.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
```

- [ ] **Step 2: Crea `packages/ui/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src", "vitest.setup.ts"]
}
```

- [ ] **Step 3: Crea `packages/ui/eslint.config.mjs`**

```js
import js from "@eslint/js";
import tseslint from "typescript-eslint";

export default tseslint.config(js.configs.recommended, ...tseslint.configs.recommended, {
  ignores: ["dist/**"],
});
```

- [ ] **Step 4: Crea `packages/ui/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "jsdom",
    setupFiles: ["./vitest.setup.ts"],
    globals: true,
  },
});
```

- [ ] **Step 5: Crea `packages/ui/vitest.setup.ts`**

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 6: Installa le dipendenze del nuovo package**

Run: `pnpm install`
Expected: completa senza errori, `packages/ui` risulta collegato nel workspace (`pnpm -r list --depth -1` lo mostra).

- [ ] **Step 7: Scrivi il test che fallisce per `tailwindPreset`**

Crea `packages/ui/src/tailwind-preset.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { tailwindPreset } from "./tailwind-preset";

describe("tailwindPreset", () => {
  it("defines the CrewMate accent color", () => {
    expect(tailwindPreset.theme?.extend?.colors).toMatchObject({
      accent: { DEFAULT: "#3fc9ff", strong: "#1a8fd4" },
    });
  });

  it("defines the title and body font families", () => {
    const fontFamily = tailwindPreset.theme?.extend?.fontFamily as Record<string, string[]>;
    expect(fontFamily.title[0]).toBe('"Press Start 2P"');
    expect(fontFamily.body[0]).toBe("Segoe UI");
  });
});
```

- [ ] **Step 8: Esegui il test e verifica che fallisca**

Run: `pnpm --filter @crewmate/ui test`
Expected: FAIL — `./tailwind-preset` non esiste ancora (errore di risoluzione modulo).

- [ ] **Step 9: Crea i design tokens CSS**

Crea `packages/ui/src/theme/tokens.css`:

```css
:root {
  --bg-900: #060d1c;
  --bg-800: #0a1428;
  --bg-700: #0d1c38;
  --bg-600: #122645;
  --surface: #10203d;
  --surface-alt: #16294a;

  --accent: #3fc9ff;
  --accent-strong: #1a8fd4;
  --accent-soft: rgba(63, 201, 255, 0.15);
  --green: #5fd35f;
  --green-strong: #3fae3f;

  --text: #e8f4ff;
  --text-muted: #9fb3cc;
  --text-dim: #6b7f99;

  --border: rgba(63, 201, 255, 0.18);
  --border-strong: rgba(63, 201, 255, 0.4);

  --danger: #ff5c5c;
  --warning: #ffcf5c;
  --success: #5fd35f;

  --radius: 10px;
  --radius-lg: 16px;
  --shadow: 0 8px 30px rgba(0, 0, 0, 0.45);
  --shadow-glow: 0 0 24px rgba(63, 201, 255, 0.25);

  --font-title: "Press Start 2P", "Segoe UI", system-ui, sans-serif;
  --font-body: "Segoe UI", system-ui, -apple-system, Roboto, Helvetica, Arial, sans-serif;

  --maxw: 1180px;
  --header-h: 72px;
}
```

- [ ] **Step 10: Crea il Tailwind preset**

Crea `packages/ui/src/tailwind-preset.ts`:

```ts
import type { Config } from "tailwindcss";

export const tailwindPreset: Partial<Config> = {
  theme: {
    extend: {
      colors: {
        bg: {
          900: "#060d1c",
          800: "#0a1428",
          700: "#0d1c38",
          600: "#122645",
        },
        surface: {
          DEFAULT: "#10203d",
          alt: "#16294a",
        },
        accent: {
          DEFAULT: "#3fc9ff",
          strong: "#1a8fd4",
          soft: "rgba(63, 201, 255, 0.15)",
        },
        green: {
          DEFAULT: "#5fd35f",
          strong: "#3fae3f",
        },
        text: {
          DEFAULT: "#e8f4ff",
          muted: "#9fb3cc",
          dim: "#6b7f99",
        },
        danger: "#ff5c5c",
        warning: "#ffcf5c",
        success: "#5fd35f",
      },
      borderRadius: {
        DEFAULT: "10px",
        lg: "16px",
      },
      boxShadow: {
        DEFAULT: "0 8px 30px rgba(0, 0, 0, 0.45)",
        glow: "0 0 24px rgba(63, 201, 255, 0.25)",
      },
      fontFamily: {
        title: ['"Press Start 2P"', "Segoe UI", "system-ui", "sans-serif"],
        body: ["Segoe UI", "system-ui", "-apple-system", "Roboto", "Helvetica", "Arial", "sans-serif"],
      },
      maxWidth: {
        site: "1180px",
      },
    },
  },
};

export default tailwindPreset;
```

- [ ] **Step 11: Crea il barrel `packages/ui/src/index.ts`**

```ts
export { tailwindPreset } from "./tailwind-preset";
export { default as tailwindPresetDefault } from "./tailwind-preset";
```

- [ ] **Step 12: Esegui il test e verifica che passi**

Run: `pnpm --filter @crewmate/ui test`
Expected: PASS (2 test in `tailwind-preset.test.ts`).

- [ ] **Step 13: Commit**

```bash
git add packages/ui
git commit -m "feat(ui): setup packages/ui con design tokens e tailwind preset"
```

---

### Task 3: `packages/ui` — componenti base (Button, Card, Container, SectionTitle, Badge)

**Files:**
- Create: `packages/ui/src/components/Button.tsx`
- Create: `packages/ui/src/components/Card.tsx`
- Create: `packages/ui/src/components/Container.tsx`
- Create: `packages/ui/src/components/SectionTitle.tsx`
- Create: `packages/ui/src/components/Badge.tsx`
- Test: `packages/ui/src/components/Button.test.tsx`
- Test: `packages/ui/src/components/components.test.tsx`
- Modify: `packages/ui/src/index.ts`

**Interfaces:**
- Consumes: setup Vitest/jsdom (Task 2).
- Produces: `Button` (props `ButtonProps { variant?: "primary" | "outline"; size?: "md" | "lg"; children: ReactNode } & ButtonHTMLAttributes<HTMLButtonElement>`), `Card` (props `CardProps { icon?: ReactNode; title?: ReactNode; children: ReactNode } & HTMLAttributes<HTMLDivElement>`), `Container` (props `ContainerProps { children: ReactNode } & HTMLAttributes<HTMLDivElement>`), `SectionTitle` (props `SectionTitleProps { subtitle?: ReactNode; children: ReactNode } & HTMLAttributes<HTMLHeadingElement>`), `Badge` (props `BadgeProps { tone?: "green" | "danger" | "warning"; children: ReactNode } & HTMLAttributes<HTMLSpanElement>`) — tutti esportati da `packages/ui/src/index.ts`, usati da `apps/web/app/page.tsx` e `apps/admin/app/page.tsx` (Task 5, 6).

- [ ] **Step 1: Scrivi i test che falliscono per `Button`**

Crea `packages/ui/src/components/Button.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { Button } from "./Button";

describe("Button", () => {
  it("renders children text", () => {
    render(<Button>Gioca Ora</Button>);
    expect(screen.getByRole("button", { name: "Gioca Ora" })).toBeInTheDocument();
  });

  it("applies primary variant classes by default", () => {
    render(<Button>Default</Button>);
    expect(screen.getByRole("button", { name: "Default" }).className).toContain("bg-accent");
  });

  it("applies outline variant classes", () => {
    render(<Button variant="outline">Visita lo Shop</Button>);
    expect(screen.getByRole("button", { name: "Visita lo Shop" }).className).toContain(
      "border-accent"
    );
  });
});
```

- [ ] **Step 2: Scrivi i test che falliscono per `Card`, `Container`, `SectionTitle`, `Badge`**

Crea `packages/ui/src/components/components.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { Badge } from "./Badge";
import { Card } from "./Card";
import { Container } from "./Container";
import { SectionTitle } from "./SectionTitle";

describe("Card", () => {
  it("renders title and children", () => {
    render(<Card title="Survival">Sopravvivi ed esplora</Card>);
    expect(screen.getByText("Survival")).toBeInTheDocument();
    expect(screen.getByText("Sopravvivi ed esplora")).toBeInTheDocument();
  });
});

describe("Container", () => {
  it("renders children inside a max-width wrapper", () => {
    render(<Container>contenuto</Container>);
    expect(screen.getByText("contenuto").className).toContain("max-w-site");
  });
});

describe("SectionTitle", () => {
  it("renders title and optional subtitle", () => {
    render(<SectionTitle subtitle="Sottotitolo">Titolo</SectionTitle>);
    expect(screen.getByText("Titolo")).toBeInTheDocument();
    expect(screen.getByText("Sottotitolo")).toBeInTheDocument();
  });
});

describe("Badge", () => {
  it("applies tone classes", () => {
    render(<Badge tone="danger">Offline</Badge>);
    expect(screen.getByText("Offline").className).toContain("text-danger");
  });
});
```

- [ ] **Step 3: Esegui i test e verifica che falliscano**

Run: `pnpm --filter @crewmate/ui test`
Expected: FAIL — nessuno dei file componente esiste ancora.

- [ ] **Step 4: Implementa `Button`**

Crea `packages/ui/src/components/Button.tsx`:

```tsx
import type { ButtonHTMLAttributes, ReactNode } from "react";

export type ButtonVariant = "primary" | "outline";
export type ButtonSize = "md" | "lg";

export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant;
  size?: ButtonSize;
  children: ReactNode;
}

const VARIANT_CLASSES: Record<ButtonVariant, string> = {
  primary: "bg-accent text-bg-900 hover:bg-accent-strong",
  outline: "border border-accent text-accent hover:bg-accent-soft",
};

const SIZE_CLASSES: Record<ButtonSize, string> = {
  md: "px-4 py-2 text-sm",
  lg: "px-6 py-3 text-base",
};

export function Button({
  variant = "primary",
  size = "md",
  className = "",
  children,
  ...rest
}: ButtonProps) {
  const classes = [
    "inline-flex items-center justify-center rounded font-body font-semibold transition-colors",
    VARIANT_CLASSES[variant],
    SIZE_CLASSES[size],
    className,
  ]
    .filter(Boolean)
    .join(" ");

  return (
    <button className={classes} {...rest}>
      {children}
    </button>
  );
}
```

- [ ] **Step 5: Implementa `Card`**

Crea `packages/ui/src/components/Card.tsx`:

```tsx
import type { HTMLAttributes, ReactNode } from "react";

export interface CardProps extends HTMLAttributes<HTMLDivElement> {
  icon?: ReactNode;
  title?: ReactNode;
  children: ReactNode;
}

export function Card({ icon, title, children, className = "", ...rest }: CardProps) {
  const classes = ["rounded-lg border border-accent-soft bg-surface p-6 shadow", className]
    .filter(Boolean)
    .join(" ");

  return (
    <div className={classes} {...rest}>
      {icon ? <div className="mb-3 text-3xl">{icon}</div> : null}
      {title ? <h3 className="mb-2 font-title text-text">{title}</h3> : null}
      {children}
    </div>
  );
}
```

- [ ] **Step 6: Implementa `Container`**

Crea `packages/ui/src/components/Container.tsx`:

```tsx
import type { HTMLAttributes, ReactNode } from "react";

export interface ContainerProps extends HTMLAttributes<HTMLDivElement> {
  children: ReactNode;
}

export function Container({ children, className = "", ...rest }: ContainerProps) {
  const classes = ["mx-auto w-full max-w-site px-5", className].filter(Boolean).join(" ");
  return (
    <div className={classes} {...rest}>
      {children}
    </div>
  );
}
```

- [ ] **Step 7: Implementa `SectionTitle`**

Crea `packages/ui/src/components/SectionTitle.tsx`:

```tsx
import type { HTMLAttributes, ReactNode } from "react";

export interface SectionTitleProps extends HTMLAttributes<HTMLHeadingElement> {
  subtitle?: ReactNode;
  children: ReactNode;
}

export function SectionTitle({ subtitle, children, className = "", ...rest }: SectionTitleProps) {
  return (
    <div className="mb-10 text-center">
      <h2 className={["font-title text-xl text-text", className].filter(Boolean).join(" ")} {...rest}>
        {children}
      </h2>
      {subtitle ? <p className="mt-3 text-text-muted">{subtitle}</p> : null}
    </div>
  );
}
```

- [ ] **Step 8: Implementa `Badge`**

Crea `packages/ui/src/components/Badge.tsx`:

```tsx
import type { HTMLAttributes, ReactNode } from "react";

export type BadgeTone = "green" | "danger" | "warning";

export interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  tone?: BadgeTone;
  children: ReactNode;
}

const TONE_CLASSES: Record<BadgeTone, string> = {
  green: "bg-green/15 text-green",
  danger: "bg-danger/15 text-danger",
  warning: "bg-warning/15 text-warning",
};

export function Badge({ tone = "green", children, className = "", ...rest }: BadgeProps) {
  const classes = [
    "inline-flex items-center gap-1 rounded-full px-3 py-1 text-xs font-semibold",
    TONE_CLASSES[tone],
    className,
  ]
    .filter(Boolean)
    .join(" ");

  return (
    <span className={classes} {...rest}>
      {children}
    </span>
  );
}
```

- [ ] **Step 9: Aggiorna il barrel `packages/ui/src/index.ts`**

```ts
export { tailwindPreset } from "./tailwind-preset";
export { default as tailwindPresetDefault } from "./tailwind-preset";

export { Button } from "./components/Button";
export type { ButtonProps, ButtonVariant, ButtonSize } from "./components/Button";

export { Card } from "./components/Card";
export type { CardProps } from "./components/Card";

export { Container } from "./components/Container";
export type { ContainerProps } from "./components/Container";

export { SectionTitle } from "./components/SectionTitle";
export type { SectionTitleProps } from "./components/SectionTitle";

export { Badge } from "./components/Badge";
export type { BadgeProps, BadgeTone } from "./components/Badge";
```

- [ ] **Step 10: Esegui i test e verifica che passino**

Run: `pnpm --filter @crewmate/ui test`
Expected: PASS su tutti i test (`tailwind-preset.test.ts`, `Button.test.tsx`, `components.test.tsx`).

- [ ] **Step 11: Commit**

```bash
git add packages/ui
git commit -m "feat(ui): aggiunge componenti base Button, Card, Container, SectionTitle, Badge"
```

---

### Task 4: `packages/db` — client Supabase tipizzato

**Files:**
- Create: `packages/db/package.json`
- Create: `packages/db/tsconfig.json`
- Create: `packages/db/eslint.config.mjs`
- Create: `packages/db/vitest.config.ts`
- Create: `packages/db/src/database.types.ts`
- Create: `packages/db/src/client.ts`
- Test: `packages/db/src/client.test.ts`
- Create: `packages/db/src/index.ts`

**Interfaces:**
- Consumes: `tsconfig.base.json` (Task 1).
- Produces: `createSupabaseClient(env: SupabaseEnv): SupabaseClient<Database>` e tipo `SupabaseEnv { url: string; anonKey: string }`, esportati da `packages/db/src/index.ts` — usati (import, non ancora invocati con env reali) da `apps/web` e `apps/admin` (Task 5, 6) tramite dipendenza `workspace:*`.

- [ ] **Step 1: Crea `packages/db/package.json`**

```json
{
  "name": "@crewmate/db",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^2.1.8"
  }
}
```

- [ ] **Step 2: Crea `packages/db/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Crea `packages/db/eslint.config.mjs`**

```js
import js from "@eslint/js";
import tseslint from "typescript-eslint";

export default tseslint.config(js.configs.recommended, ...tseslint.configs.recommended, {
  ignores: ["dist/**"],
});
```

- [ ] **Step 4: Crea `packages/db/vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    globals: true,
  },
});
```

- [ ] **Step 5: Installa le dipendenze del nuovo package**

Run: `pnpm install`
Expected: completa senza errori, `@crewmate/db` risulta nel workspace.

- [ ] **Step 6: Crea il placeholder dei tipi generati**

Crea `packages/db/src/database.types.ts`:

```ts
// Placeholder: nessun progetto Supabase reale ancora creato (vedi docs/SETUP.md).
// Una volta creato il progetto, rigenera con:
//   supabase gen types typescript --project-id <project-id> > src/database.types.ts
export type Database = Record<string, never>;
```

- [ ] **Step 7: Scrivi il test che fallisce per `createSupabaseClient`**

Crea `packages/db/src/client.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createSupabaseClient } from "./client";

describe("createSupabaseClient", () => {
  it("creates a client when url and anonKey are provided", () => {
    const client = createSupabaseClient({
      url: "https://example.supabase.co",
      anonKey: "anon-key",
    });
    expect(client).toBeTruthy();
    expect(typeof client.from).toBe("function");
  });

  it("throws when url is missing", () => {
    expect(() => createSupabaseClient({ url: "", anonKey: "anon-key" })).toThrow(
      "createSupabaseClient: url e anonKey sono obbligatori"
    );
  });

  it("throws when anonKey is missing", () => {
    expect(() =>
      createSupabaseClient({ url: "https://example.supabase.co", anonKey: "" })
    ).toThrow("createSupabaseClient: url e anonKey sono obbligatori");
  });
});
```

- [ ] **Step 8: Esegui il test e verifica che fallisca**

Run: `pnpm --filter @crewmate/db test`
Expected: FAIL — `./client` non esiste ancora.

- [ ] **Step 9: Implementa `createSupabaseClient`**

Crea `packages/db/src/client.ts`:

```ts
import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "./database.types";

export interface SupabaseEnv {
  url: string;
  anonKey: string;
}

export function createSupabaseClient(env: SupabaseEnv): SupabaseClient<Database> {
  if (!env.url || !env.anonKey) {
    throw new Error("createSupabaseClient: url e anonKey sono obbligatori");
  }
  return createClient<Database>(env.url, env.anonKey);
}
```

- [ ] **Step 10: Crea il barrel `packages/db/src/index.ts`**

```ts
export { createSupabaseClient } from "./client";
export type { SupabaseEnv } from "./client";
export type { Database } from "./database.types";
```

- [ ] **Step 11: Esegui il test e verifica che passi**

Run: `pnpm --filter @crewmate/db test`
Expected: PASS (3 test in `client.test.ts`).

- [ ] **Step 12: Commit**

```bash
git add packages/db
git commit -m "feat(db): aggiunge client Supabase tipizzato con placeholder dei tipi"
```

---

### Task 5: `apps/web` — scaffold Next.js

**Files:**
- Create: `apps/web/package.json`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/next-env.d.ts`
- Create: `apps/web/next.config.ts`
- Create: `apps/web/postcss.config.js`
- Create: `apps/web/tailwind.config.ts`
- Create: `apps/web/eslint.config.mjs`
- Create: `apps/web/app/globals.css`
- Create: `apps/web/app/layout.tsx`
- Create: `apps/web/app/page.tsx`
- Create: `apps/web/.env.example`

**Interfaces:**
- Consumes: `tailwindPreset` da `@crewmate/ui` (Task 2), `Badge`/`Button`/`Card`/`Container`/`SectionTitle` da `@crewmate/ui` (Task 3), `createSupabaseClient`/`SupabaseEnv` da `@crewmate/db` (Task 4, importato ma non ancora invocato con env reali).
- Produces: app Next.js avviabile su `http://localhost:3000` che dimostra visivamente il design system collegato.

- [ ] **Step 1: Crea `apps/web/package.json`**

```json
{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "echo \"nessun test in questa fase\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crewmate/db": "workspace:*",
    "@crewmate/ui": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.2.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "autoprefixer": "^10.4.20",
    "eslint": "^9.17.0",
    "eslint-config-next": "^15.1.3",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 2: Crea `apps/web/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "preserve",
    "module": "esnext",
    "moduleResolution": "bundler",
    "noEmit": true,
    "isolatedModules": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: Crea `apps/web/next-env.d.ts`**

```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />
```

- [ ] **Step 4: Crea `apps/web/next.config.ts`**

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  transpilePackages: ["@crewmate/ui", "@crewmate/db"],
};

export default nextConfig;
```

- [ ] **Step 5: Crea `apps/web/postcss.config.js`**

```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 6: Crea `apps/web/tailwind.config.ts`**

```ts
import type { Config } from "tailwindcss";
import { tailwindPreset } from "@crewmate/ui";

const config: Config = {
  presets: [tailwindPreset as Config],
  content: ["./app/**/*.{ts,tsx}", "../../packages/ui/src/**/*.{ts,tsx}"],
};

export default config;
```

- [ ] **Step 7: Crea `apps/web/eslint.config.mjs`**

```js
import { FlatCompat } from "@eslint/eslintrc";

const compat = new FlatCompat({ baseDirectory: import.meta.dirname });

const eslintConfig = [...compat.extends("next/core-web-vitals", "next/typescript")];

export default eslintConfig;
```

- [ ] **Step 8: Crea `apps/web/app/globals.css`**

```css
@import "../../../packages/ui/src/theme/tokens.css";

@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 9: Crea `apps/web/app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import type { ReactNode } from "react";
import "./globals.css";

export const metadata: Metadata = {
  title: "CrewMate Network — Server Minecraft",
  description:
    "CrewMate Network: il server Minecraft dove costruire, giocare e crescere insieme alla community.",
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="it">
      <body className="min-h-screen bg-bg-900 font-body text-text">{children}</body>
    </html>
  );
}
```

- [ ] **Step 10: Crea `apps/web/app/page.tsx`**

```tsx
import { Badge, Button, Card, Container, SectionTitle } from "@crewmate/ui";

export default function Home() {
  return (
    <Container>
      <div className="flex flex-col items-center gap-6 py-20 text-center">
        <Badge tone="green">Server Online</Badge>
        <h1 className="font-title text-2xl text-text">CrewMate Network</h1>
        <div className="flex gap-4">
          <Button>Gioca Ora</Button>
          <Button variant="outline">Visita lo Shop</Button>
        </div>
      </div>
      <SectionTitle subtitle="Verifica che il design system di packages/ui sia collegato correttamente.">
        Design System — Fase 0
      </SectionTitle>
      <div className="grid gap-6 pb-20 sm:grid-cols-3">
        <Card icon="⛏️" title="Survival">
          Sopravvivi, costruisci ed esplora un mondo persistente.
        </Card>
        <Card icon="🏝️" title="SkyBlock">
          Parti da un&apos;isola nel vuoto e costruisci il tuo impero.
        </Card>
        <Card icon="⚔️" title="Minigames">
          Sfida gli altri giocatori in modalità competitive.
        </Card>
      </div>
    </Container>
  );
}
```

- [ ] **Step 11: Crea `apps/web/.env.example`**

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

- [ ] **Step 12: Installa le dipendenze e builda**

Run: `pnpm install && pnpm --filter web build`
Expected: build completa senza errori TypeScript/ESLint bloccanti.

- [ ] **Step 13: Verifica visivamente in dev**

Run: `pnpm --filter web dev` (poi apri `http://localhost:3000`)
Expected: pagina con sfondo navy, badge "Server Online" verde, due bottoni (pieno ciano / outline), titolo in font pixel, tre card Survival/SkyBlock/Minigames. Ferma il server dopo la verifica (Ctrl+C).

- [ ] **Step 14: Commit**

```bash
git add apps/web
git commit -m "feat(web): scaffold Next.js con design system collegato"
```

---

### Task 6: `apps/admin` — scaffold Next.js

**Files:**
- Create: `apps/admin/package.json`
- Create: `apps/admin/tsconfig.json`
- Create: `apps/admin/next-env.d.ts`
- Create: `apps/admin/next.config.ts`
- Create: `apps/admin/postcss.config.js`
- Create: `apps/admin/tailwind.config.ts`
- Create: `apps/admin/eslint.config.mjs`
- Create: `apps/admin/app/globals.css`
- Create: `apps/admin/app/layout.tsx`
- Create: `apps/admin/app/page.tsx`
- Create: `apps/admin/.env.example`

**Interfaces:**
- Consumes: `tailwindPreset`, `Badge`, `Container`, `SectionTitle` da `@crewmate/ui` (Task 2, 3); `createSupabaseClient`/`SupabaseEnv` da `@crewmate/db` (Task 4, importato, non invocato).
- Produces: app Next.js avviabile su una porta separata (`http://localhost:3001`) che dimostra il design system collegato anche nell'admin. Nessun guard di autenticazione (arriva in Fase 3).

- [ ] **Step 1: Crea `apps/admin/package.json`**

```json
{
  "name": "admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "lint": "eslint .",
    "test": "echo \"nessun test in questa fase\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crewmate/db": "workspace:*",
    "@crewmate/ui": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.2.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "autoprefixer": "^10.4.20",
    "eslint": "^9.17.0",
    "eslint-config-next": "^15.1.3",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  }
}
```

- [ ] **Step 2: Crea `apps/admin/tsconfig.json`** (identico a `apps/web/tsconfig.json`)

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "preserve",
    "module": "esnext",
    "moduleResolution": "bundler",
    "noEmit": true,
    "isolatedModules": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: Crea `apps/admin/next-env.d.ts`**

```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />
```

- [ ] **Step 4: Crea `apps/admin/next.config.ts`**

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  transpilePackages: ["@crewmate/ui", "@crewmate/db"],
};

export default nextConfig;
```

- [ ] **Step 5: Crea `apps/admin/postcss.config.js`**

```js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 6: Crea `apps/admin/tailwind.config.ts`**

```ts
import type { Config } from "tailwindcss";
import { tailwindPreset } from "@crewmate/ui";

const config: Config = {
  presets: [tailwindPreset as Config],
  content: ["./app/**/*.{ts,tsx}", "../../packages/ui/src/**/*.{ts,tsx}"],
};

export default config;
```

- [ ] **Step 7: Crea `apps/admin/eslint.config.mjs`**

```js
import { FlatCompat } from "@eslint/eslintrc";

const compat = new FlatCompat({ baseDirectory: import.meta.dirname });

const eslintConfig = [...compat.extends("next/core-web-vitals", "next/typescript")];

export default eslintConfig;
```

- [ ] **Step 8: Crea `apps/admin/app/globals.css`**

```css
@import "../../../packages/ui/src/theme/tokens.css";

@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 9: Crea `apps/admin/app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import type { ReactNode } from "react";
import "./globals.css";

export const metadata: Metadata = {
  title: "CrewMate Network — Admin",
  description: "Pannello di amministrazione CrewMate Network.",
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="it">
      <body className="min-h-screen bg-bg-900 font-body text-text">{children}</body>
    </html>
  );
}
```

- [ ] **Step 10: Crea `apps/admin/app/page.tsx`**

```tsx
import { Badge, Container, SectionTitle } from "@crewmate/ui";

export default function AdminHome() {
  return (
    <Container>
      <div className="flex flex-col items-center gap-6 py-20 text-center">
        <Badge tone="warning">Scaffold Fase 0</Badge>
        <SectionTitle subtitle="Il guard di autenticazione (role = admin) arriva in Fase 3.">
          Pannello Admin — CrewMate Network
        </SectionTitle>
      </div>
    </Container>
  );
}
```

- [ ] **Step 11: Crea `apps/admin/.env.example`**

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RCON_HOST=
RCON_PORT=25575
RCON_PASSWORD=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
```

- [ ] **Step 12: Installa le dipendenze e builda**

Run: `pnpm install && pnpm --filter admin build`
Expected: build completa senza errori TypeScript/ESLint bloccanti.

- [ ] **Step 13: Verifica visivamente in dev**

Run: `pnpm --filter admin dev` (poi apri `http://localhost:3001`)
Expected: pagina con badge "Scaffold Fase 0" (giallo/warning) e titolo "Pannello Admin — CrewMate Network" con sottotitolo. Ferma il server dopo la verifica (Ctrl+C).

- [ ] **Step 14: Commit**

```bash
git add apps/admin
git commit -m "feat(admin): scaffold Next.js con design system collegato"
```

---

### Task 7: Istruzioni di setup manuale (`docs/SETUP.md`)

**Files:**
- Create: `docs/SETUP.md`

**Interfaces:**
- Consumes: `apps/web/.env.example`, `apps/admin/.env.example` (Task 5, 6) come riferimento per le variabili da compilare.
- Produces: documento di istruzioni manuali, nessuna interfaccia di codice.

- [ ] **Step 1: Crea `docs/SETUP.md`**

```markdown
# Setup manuale — Supabase & Vercel

Questi passi vanno fatti a mano dall'admin del progetto (non dal codice).
Servono per collegare le fasi successive a un progetto reale.

## 1. Supabase

1. Crea un account su https://supabase.com se non ne hai già uno.
2. Crea un nuovo progetto, **regione EU** (es. `eu-central-1`), come da
   `docs/BRIEF.md` §3.
3. Copia da *Project Settings → API*:
   - `Project URL` → va in `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public key` → va in `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role key` → va in `SUPABASE_SERVICE_ROLE_KEY` (**solo** in
     `apps/admin/.env.local`, mai nel client, mai committata)
4. Compila `apps/web/.env.local` e `apps/admin/.env.local` copiando
   `.env.example` e incollando i valori reali (questi file sono già
   esclusi da git tramite `.gitignore`).
5. Le tabelle del database (`profiles`, `products`, ecc. — vedi
   `docs/BRIEF.md` §4) verranno create nella Fase 1 in poi, con le
   relative migrazioni.

## 2. Vercel

1. Crea un account su https://vercel.com se non ne hai già uno.
2. Crea **due** progetti Vercel collegati a questo repository:
   - uno con Root Directory `apps/web` (dominio principale)
   - uno con Root Directory `apps/admin` (sottodominio `admin.`)
3. In entrambi i progetti, aggiungi le stesse variabili d'ambiente dei
   rispettivi `.env.example` nelle impostazioni del progetto Vercel
   (Environment Variables), sia per Production che Preview.
4. Il deploy vero e proprio è previsto in Fase 10 (go-live) — questi
   passi preparano solo i progetti.

## 3. Discord OAuth (per la Fase 1)

1. Vai su https://discord.com/developers/applications e crea una nuova
   applicazione.
2. In *OAuth2*, copia `Client ID` e `Client Secret` → vanno in
   `DISCORD_CLIENT_ID` e `DISCORD_CLIENT_SECRET` (solo in
   `apps/admin/.env.local`, usati lato server per il flusso OAuth
   configurato in Supabase Auth).
3. Configura il provider Discord in Supabase (*Authentication →
   Providers → Discord*) con questi stessi valori.

## 4. RCON (per la Fase 6)

Non serve ancora in questa fase. Quando il server Minecraft sarà pronto
per i test di consegna, compila `RCON_HOST`, `RCON_PORT`,
`RCON_PASSWORD` in `apps/admin/.env.local`.
```

- [ ] **Step 2: Commit**

```bash
git add docs/SETUP.md
git commit -m "docs: aggiunge istruzioni di setup manuale per Supabase, Vercel e Discord OAuth"
```

---

### Task 8: Verifica finale del monorepo

**Files:**
- Nessun file nuovo — solo verifica end-to-end.

**Interfaces:**
- Consumes: tutti i task precedenti.
- Produces: conferma che l'intero monorepo passa `build`, `lint`, `test`, `typecheck` orchestrati da Turborepo, e che le pagine statiche esistenti non sono state toccate.

- [ ] **Step 1: Installazione pulita**

Run: `pnpm install`
Expected: nessun errore, `pnpm-lock.yaml` aggiornato.

- [ ] **Step 2: Build di tutto il monorepo**

Run: `pnpm build`
Expected: `packages/ui`, `packages/db`, `apps/web`, `apps/admin` completano senza errori (Turborepo esegue in ordine di dipendenza).

- [ ] **Step 3: Lint di tutto il monorepo**

Run: `pnpm lint`
Expected: nessun errore ESLint bloccante in nessun package/app.

- [ ] **Step 4: Typecheck di tutto il monorepo**

Run: `pnpm typecheck`
Expected: nessun errore TypeScript in nessun package/app.

- [ ] **Step 5: Test di tutto il monorepo**

Run: `pnpm test`
Expected: tutti i test Vitest di `packages/ui` e `packages/db` passano (`apps/web`/`apps/admin` non hanno test in questa fase, script no-op).

- [ ] **Step 6: Verifica che le pagine statiche esistenti non siano state toccate**

Run: `git status assets/ index.html shop.html forum.html candidature.html README.md`
Expected: nessuna modifica rispetto allo stato pre-esistente di questo piano (a parte eventuali modifiche già in corso prima di iniziare, non introdotte da questo piano).

- [ ] **Step 7: Commit finale (se necessario)**

Se `pnpm install` ha aggiornato `pnpm-lock.yaml` in modo che non sia già stato committato:

```bash
git add pnpm-lock.yaml
git commit -m "chore: aggiorna lockfile dopo verifica finale Fase 0"
```

---

## Self-Review (svolta durante la stesura del piano)

- **Copertura spec:** struttura monorepo (Task 1), `packages/ui` con token/preset (Task 2) e componenti (Task 3), `packages/db` (Task 4), `apps/web` (Task 5), `apps/admin` (Task 6), env + `docs/SETUP.md` (Task 7), verifica (Task 8) — tutte le sezioni "In scope" della spec sono coperte. Le sezioni "Fuori scope" (porting pagine reali, header/footer dinamici, guard admin, tabelle Supabase) non sono toccate da nessun task, come da spec.
- **Placeholder:** nessun "TBD"/"TODO" applicativo; l'unico commento "placeholder" (`database.types.ts`) è intenzionale e descritto nella spec stessa.
- **Coerenza tipi:** `SupabaseEnv { url, anonKey }` e `createSupabaseClient` (Task 4) sono lo stesso nome/firma ovunque referenziati (Task 5, 6). `tailwindPreset` esportato in Task 2, consumato con lo stesso nome in Task 5/6. Tutti i componenti (`Button`, `Card`, `Container`, `SectionTitle`, `Badge`) hanno nomi coerenti tra Task 3 (definizione) e Task 5/6 (uso in `page.tsx`).
