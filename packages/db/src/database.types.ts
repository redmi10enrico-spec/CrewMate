// Placeholder: nessun progetto Supabase reale ancora creato (vedi docs/SETUP.md).
// Una volta creato il progetto, rigenera con:
//   supabase gen types typescript --project-id <project-id> > src/database.types.ts
export type Database = Record<string, never>;
